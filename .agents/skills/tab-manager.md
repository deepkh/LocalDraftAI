# `src/js/tab-manager.js`

- Change in-memory document tab ordering, active tab selection, tab closing behavior, and file-handle lookup here.
- Use this file when the work affects how multiple document sessions are owned before `src/js/app.js` renders them.
